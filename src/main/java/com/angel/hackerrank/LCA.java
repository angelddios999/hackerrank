package com.angel.hackerrank;

public class LCA {
    public static Node lca(Node root, int v1, int v2) {
        Node lca = null;
        // Search v1
        // Keep a reference to ancestors of v1
        // Search for v1 on every ancestor of v1
        // If v2 is a direct ancestor of v1 return the ancestor of v2

        return lca;
    }
}
